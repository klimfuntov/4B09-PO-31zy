name: cli
description: A sample command-line application.
version: 1.0.0
resolution: workspace   # <-- добавить эту строку
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  http: ^1.3.0
  command_runner:
    path: ../command_runner
  wikipedia:
    path: ../wikipedia
  logging: ^1.2.0   # ← добавить эту строку

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
